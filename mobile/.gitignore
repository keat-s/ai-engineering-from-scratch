# Expo / React Native
node_modules/
.expo/
dist/
web-build/
*.log

# Native build output (generated by `expo prebuild`)
/ios/
/android/

# Env / secrets — never commit real AdMob or RevenueCat keys
.env
.env.local
*.keystore
google-services.json
GoogleService-Info.plist

# OS / editor
.DS_Store
